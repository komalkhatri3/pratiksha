<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.harsh.nandy.cancerantidote">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".firstpage"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".login"
            android:label="@string/title_activity_login"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Listofhospitals"
            android:label="@string/title_activity_listofhospitals"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".book_ambulance"
            android:label="@string/title_activity_book_ambulance"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".Otherservices"
            android:label="@string/title_activity_otherservices"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>